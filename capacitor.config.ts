import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'jp-study-cards',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
